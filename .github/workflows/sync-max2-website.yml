name: Legacy MAX2 website sync (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-max2-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current production route
        run: |
          echo "The hourly MAX2 publisher is disabled."
          echo "Website Production now follows the MB ALL pre-draw frozen decision."
          echo "Do not overwrite ai-methods/public-methods.json or the public website with legacy MAX2 output."
