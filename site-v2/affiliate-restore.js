(() => {
  "use strict";

  // Retired runtime. Shopee merchandising is rendered only by
  // scripts/sitewide_product_surface.py as the canonical four-card block.
  // Keep this file as a harmless compatibility stub until all older builders
  // stop referencing the asset.
})();
